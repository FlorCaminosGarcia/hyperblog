#aitk_get_agent_code_gen_best_practices Create a cooking AI agent app with GitHub models in Python. The app should leverage AI language model to support recipe search and ingredient extraction features. This should be an interactive console AI application. Generate the code to current workspace and if current workspace already contains a project, ask the user where to put the generated code.